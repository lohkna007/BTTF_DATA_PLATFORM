# config/config.env
# This file contains environment-specific configurations.
# In production on AWS, these would be set as Lambda environment variables or stored in AWS Secrets Manager.

OPEN_METEO_BASE_URL=https://archive-api.open-meteo.com/v1/archive
